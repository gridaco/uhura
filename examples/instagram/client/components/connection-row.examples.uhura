use fixture standard

example following default {
  props {
    person = fixture.connections.lena
    viewer = "user-mira"
    pending = false
  }
}

example follow-action {
  props {
    person = fixture.connections.nils
    viewer = "user-mira"
    pending = false
  }
}

example pending {
  props {
    person = fixture.connections.nils
    viewer = "user-mira"
    pending = true
  }
}
