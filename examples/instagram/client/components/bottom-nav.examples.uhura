use fixture standard

example feed-active default {
  props { current = "feed" }
}

example profile-active {
  props { current = "profile" }
}

example create-active {
  props { current = "create" }
}

example search-active {
  props { current = "search" }
}

example reels-active {
  props { current = "reels" }
}
